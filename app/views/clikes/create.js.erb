var idComment = <%= @id_comment %>;
$('a[data-correction-id="'+idComment+'"]').parent().html("<%= j(render 'off', review: @review, correction: @correction, like: @correction_like, count: @count) %>");
